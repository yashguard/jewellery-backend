import Joi from "joi";

class validation {
    /**
     * create
     */
    static create = {
        body: Joi.object().keys({

        })
    };

    /**
     * update
     */
    static update = {
        body: Joi.object().keys({

        })
    };
}

export default validation;
